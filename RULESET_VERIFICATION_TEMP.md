# Temporary

This file exists only to open a throwaway PR that exercises the `main-protection`
ruleset. It is never merged; the PR is closed and the branch deleted once the
merge box has been inspected.
