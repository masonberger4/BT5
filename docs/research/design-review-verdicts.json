[
 {
  "scores": [
   {
    "proposal": "A-domain-layered",
    "total": 29,
    "concreteness": 9,
    "parallelBuildability": 6,
    "riskManagement": 5,
    "scientificCorrectness": 9,
    "strengths": [
     "Highest scientific fidelity of the three. The D8b ZAP worked example is actually correct on the science that matters: worst-window not global count, density AND spacing, gated to lenti/AAV/mRNA only, evidence B, and weight_provenance that cites the 2021 lentiviral null result against itself. This is the only document that demonstrates a rule end-to-end rather than describing one.",
     "One-file-per-rule with filenames mapping 1:1 onto brief \u00a72 IDs (d8b_cpg_zap_window.py, f5_at_window_band.py) makes 'did we implement D5(c)?' an `ls`. This is the single most reviewable artifact in any of the three proposals and it is what lets two rules agents share a directory safely.",
     "`Segment.table_id` is PER SEGMENT, not per host \u2014 the only proposal that can represent an organelle-targeted fusion. Directly closes critique Tier-1 #2 harder than either competitor.",
     "`SegmentKind.ANNOTATED_INTRON` and `WHITELISTED_REPEAT` are the only explicit answers to critique Tier-3 #5 (splice-removal will destroy a deliberately placed intron) and to brief \u00a73.3's 'LTRs and ITRs irreducibly violate the plasmid rules'. B covers this only weakly via `Feature.immutable`; C not at all.",
     "`Violation.fixable_by_codon_choice` correctly routes B10 (uAUG in the user's own UTR) and F7 (TM-segment toxicity) to the protocol advisor instead of the solver. No competitor separates 'we found it' from 'we can fix it'.",
     "`RuleSpec.engine_calibration` refusing a \u0394G threshold when the active FoldEngine reports a different (name, param set) is the correct structural fix for brief \u00a77-tech-iii, the named most-likely correctness bug in the product.",
     "Covers two critique items the other two miss entirely: x1_oligo_match_loss (Tier-5 #7, recoding silently kills the user's qPCR primers) and x2_protein_liability (Tier-3 #6, N-glyc sequon created at a tag junction).",
     "Per-rule cost accounting (re-score with each hard rule individually relaxed) falls out of having ActiveRule as data \u2014 this is brief \u00a76 gap #8 delivered as a consequence of the type system rather than as a feature someone has to remember."
    ],
    "weaknesses": [
     "FATAL, SELF-INFLICTED: \u00a73.3 ships `packages/engine/rules_catalog.json`, regenerated from every discovered SPEC and CI-diff-gated by `rules-catalog-snapshot`. The document's central claim is 'the shared mutable list that normally causes every merge conflict does not exist.' It does exist \u2014 A just made it machine-generated and put a required CI job on it. THIS IS THE WEEK-ONE FILE: RULES-SCAN and RULES-MODEL (A's own recommended 2-way split, ~45 rules) both rewrite it on literally every PR, entries sort by rule id so the hunks interleave, and every merge red-lights every other open rules PR. The highest-volume lane is the one that serializes.",
     "SECOND WEEK-ONE FILE: `pyproject.toml` / `uv.lock`. githubPlan \u00a78.2 flags this \ud83d\udd34 as the highest-churn least-mergeable file in the repo. In week one M2 needs numpy/scipy for MaxEnt and the Salis TIR model, M3 needs pyahocorasick and seqfold, M6 needs tatapov and primer3-py, M1 needs biopython and a SnapGene reader. A never mentions dependency management once. Four lanes, one lockfile, day one.",
     "THIRD WEEK-ONE FILE: `data/MANIFEST.sha256`. A routes codon tables, PaxDb HEG sets, tRNA GCN, `data/vendors/*.toml`, and `data/elements/*.json` through `data/**`, which githubPlan label-gates `approved:data-change` and which carries one manifest file listing a hash per asset. M1, M2, M4 and M6 all need reference data in week one; every one of those PRs is owner-serialized AND collides on the manifest. A's own dependency graph draws `DATA -.reads.-> ING & RUL & CTX & ASM` and then says nothing about it.",
     "Zero CI-capacity analysis. A proposes 8 lanes 'scaling to 10' against githubPlan \u00a78.1's measured ceiling of ~4\u20135 concurrent Python PRs on a free personal account, and then adds three more CI jobs (`rules-catalog-snapshot`, `openapi-drift`, nightly `vendor-oracle`) on top of the ~12 job slots a Python PR already consumes. B and C both do this arithmetic; A does not acknowledge the constraint exists.",
     "The null-model cost claim in \u00a79.3/\u00a712 is wrong. A asserts the 400-variant empirical null is '\u22641 s' and 'no extra cost' because it reuses variants 'already sampled'. But every A evaluator takes a `ConstructRecord` \u2014 there is deliberately no bare-string path \u2014 so the null is 400 \u00d7 (every active rule on the assembled circular plasmid), Tier-C folding rules included. C is the only proposal that spots this and it needs a dedicated flag (`requires_construct`) plus a measured gate (G3) to make it affordable.",
     "`Evidence` collapses vendor-assertion and folklore into one value (`C = 'vendor_or_folklore'`) and then CI-asserts `Evidence.C \u21d2 default_enabled is False`. Brief \u00a76.8 and critique Tier-2 #8 want three distinct badges with only *folklore* defaulting off. A's collapse ships every vendor-asserted manufacturability rule \u2014 the ones vendors actually enforce at order time \u2014 disabled by default.",
     "No mechanism for being wrong. \u00a714 offers only 'an additive field on a frozen dataclass with a default is a compatible change and can land mid-flight under approved:oracle-change'. That is one owner-authored PR per discovery, with no class taxonomy, no deprecation window, no unblocking hatch, and no cadence. Compare B \u00a77, which is an entire protocol.",
     "Ambiguous weight ownership: `RuleSpec.default_weight` lives in the rule file (M2/M3) while `context/presets/*.toml` also carries per-rule weights (M4). Every new rule needs an M4 entry to appear in a preset. A resolves the *precedence* ('spec \u222a preset \u222a override') but never the *ownership*, so the default weight vector \u2014 which critique Tier-1 #3 calls 'the product' \u2014 has two owners."
    ]
   },
   {
    "proposal": "B-contract-first",
    "total": 31,
    "concreteness": 8,
    "parallelBuildability": 7,
    "riskManagement": 8,
    "scientificCorrectness": 8,
    "strengths": [
     "The Real/Recorded/Fake triple for every port, with fixtures namespaced by CONSUMING lane (`tests/fixtures/ports/folding/solver/` vs `.../rules_expression/`), is the single best anti-blocking mechanism proposed in any of the three documents. Two lanes physically cannot collide on a fixture file, and `FixtureMissing` naming the exact `--record-port` command means a stalled agent self-unblocks in seconds.",
     "The nightly `fixture-fidelity` job \u2014 re-run every recorded fixture against the real implementation, fail non-blocking, open an issue naming port/consumer/hash \u2014 is exactly right. A stale fixture must never stall a PR but must never go unnoticed. Neither competitor has drift detection on their stubs at all.",
     "Per-lane golden directories (`tests/goldens/<lane>/__snapshots__/` with per-lane `cases.json`). B is the ONLY proposal that noticed githubPlan ships a single shared `tests/goldens/__snapshots__/`, which at 8\u20139 lanes is a guaranteed conflict on every optimizer-touching PR. This is a real defect in the base plan that only B caught.",
     "`ProteinInput.translation_table_id` with no default: Pydantic physically refuses to construct the model. Combined with `HostProfile.locked_translation_table_id` and a cross-validator, this is the cleanest closure of critique Tier-1 #2 of the three \u2014 'a model with no default cannot be silently wrong' beats C's prose 'EXPLICIT. Never defaulted silently.'",
     "`FoldEnergy` carries engine, engine_version, param_set, temperature_c and dangles inside the VALUE. A \u0394G cannot travel without its provenance. This is strictly stronger than A's per-spec `engine_calibration`, because it protects numbers that were never gated by a threshold in the first place.",
     "`tools/mockserver` replaying recorded HTTP transcripts and validating BOTH directions against the frozen OpenAPI, with the *same* transcripts asserted against the real ASGI app by the server lane. Consumer-driven contract testing removes the last real sequencing edge in the system; A's committed-openapi handshake is weaker and C's `tests/fixtures/api/*.json` is one-directional.",
     "The amendment protocol (\u00a77) is the most mature governance artifact in the three. MINOR/MAJOR/CATALOGUE classes, additive-with-default fast path, two-window deprecation shim tracked by a nightly job, and `test_backward_compat` re-parsing every recorded fixture inside the amendment PR itself.",
     "\u00a77.5 is genuinely wise: `Feature.parts: tuple[Interval, ...]` instead of `Feature.location: Interval` converts what feels like a MAJOR amendment into no amendment at all. The stated lesson \u2014 spend PR #0's budget on the SHAPES of the four expensive conventions, not the COUNT of fields \u2014 is the correct generalization and neither competitor articulates it.",
     "Honest about lane count vs concurrency: nine ownership boundaries throttled to ~4\u20135 running sessions by CI slots, with the correct observation that a zero-edge graph makes the running subset arbitrary."
    ],
    "weaknesses": [
     "FATAL: `packages/contracts/src/bt5_contracts/catalogue.py` \u2014 ~70 RuleSpecs hand-transcribed from brief \u00a72, in ONE owner-owned frozen file, BEFORE a single rule is implemented. THIS IS THE WEEK-ONE FILE: M2 (rules/expression) and M3 (rules/build) both need it from hour one and NEITHER MAY EDIT IT. And the block is hard, not soft: B states 'an implementation must match an existing spec id', so a rule whose spec is wrong cannot ship at all until the owner merges an amendment.",
     "B's own \u00a710 risk #1 concedes `RuleKind` may need a fifth value for windowed GC \u2014 i.e. the field that determines solver dispatch, fixed for 70 rules on day zero, is admitted to be probably wrong. Getting `kind`, `objective_id`, `hosts`, `delivery`, `conflicts_with` and `citations` right for D3 splice, F5 AT-window, B3 the Bo\u00ebl dual gate, and C9 CPB before implementing any of them is exactly the thing you cannot do. Realistically a third of the catalogue amends in weeks 1\u20133, all through one owner, on one file, with M2/M3/M4/M8/M9 downstream.",
     "The `ext` escape hatch \u2014 B's marquee 'zero blocked minutes' device \u2014 does not cover the case that blocks people. `ext` is namespaced per writing lane and 'no lane may read another lane's namespace', enforced by a contract test. So `ext` works only when producer and consumer are the SAME lane. Both worked examples (\u00a77.4 M5 adding dangles, \u00a77.5 M6 and join()) are intra-lane. The blocking case is lane X needing a field lane Y produces, which is precisely what `ext` forbids.",
     "SECOND WEEK-ONE FILE: `tests/fixtures/http/<route>/<case>.json`. B carefully namespaces port fixtures by consumer lane and then leaves the HTTP transcripts as a flat shared directory that M8 records into and M9 replays and both assert against. It is the one fixture directory two lanes both write, and B's own \u00a78.1 argument explains why that is fatal.",
     "THIRD WEEK-ONE FILE: `packages/contracts-ts/src/generated.ts` (plus `schema/v1/openapi.json` and `apps/web/src/api/generated/`). Three committed generated artifacts, all rewritten by every contract amendment, all diff-gated by `contract-freeze` as a HARD FAIL. Every MINOR amendment \u2014 which B expects to be frequent and cheap \u2014 red-lights every open M8 and M9 PR simultaneously.",
     "`pyproject.toml` / `uv.lock` is unaddressed. B's PR #0 checklist does not list it, and \u00a79's wave-1 opening command is `uv sync --locked`, so the lock exists but there is no pre-declaration policy. Same day-one four-lane collision as A. B analyzes CI concurrency carefully and then misses githubPlan's other \ud83d\udd34.",
     "`RuleKind` conflates *what a rule means* with *which solver tier runs it* (LATTICE_MOTIF / WINDOW / GLOBAL / STRUCTURE). That is an implementation taxonomy frozen into the science layer. Brief \u00a72.D3 explicitly requires splice-donor removal to ITERATE TO A FIXED POINT ('point-mutating one donor activates cryptic donors nearby... a single-pass remove-motifs step is unsafe'); there is no `RuleKind` for that and B's Tier-B dispatch has no convergence contract.",
     "`DesignRequest.screen_token` as a required HMAC field is structurally elegant but slightly theatrical \u2014 the HMAC key lives in the same local offline process, so any caller who can construct a request can mint a token. It forces the call path, which is the real win, but B oversells it as a gate.",
     "Nine lanes is the wrong answer to a question that asked for 6\u20138, and B's proposed collapse (merge M2+M3) is the one merge that does NOT reduce contention, because both already contend on catalogue.py rather than on each other."
    ]
   },
   {
    "proposal": "C-vertical-slice",
    "total": 33,
    "concreteness": 9,
    "parallelBuildability": 6,
    "riskManagement": 10,
    "scientificCorrectness": 8,
    "strengths": [
     "Risk R4 \u2014 the candidate gallery degenerating because a \u03bb-sweep over a weighted sum recovers only supported (convex-hull) Pareto points, so 24 \u03bb-points collapse to 3 near-identical sequences \u2014 is the single best insight in the three documents. Brief \u00a74 flags the math; critique Tier-4 flags the presentation problem; NOBODY connected them to 'the product's central claim is false and you find out after the UI is built.' A and B both build a candidate-gallery UI on top of this untested assumption.",
     "G0\u2013G7 are seven numeric go/no-go gates with pass thresholds AND named fallbacks, run against a working skeleton before agent #4 starts. G1 (Tier-A DP \u226450 ms, block-state \u22642 s), G2 (planted BsaI across the insert/backbone junction AND across the origin), G4 (pairwise codon distance \u226515%), G5 (junction_trap raises with \u22643 spec ids and an interval \u226410 codons). This is the only proposal where the interface is *measured* rather than *asserted*.",
     "M6 (\u00a75) is the best answer to the actual question asked. It is the only place in the three documents that enumerates the shared mutable files and pre-registers them: every third-party dependency declared in `pyproject.toml` in PR #0 (biopython, numpy, pyahocorasick, seqfold, primer3-py, pydantic, fastapi, tatapov, pyyaml, viennarna/commec as extras), every planned benchmark metric name pre-registered in `tolerances.yaml` with `blocking: false`, the full panel in `panel.json` up front, generated TS types produced into `apps/web/` by a script the WEB lane owns so the server lane never writes there.",
     "Splitting the `data/**` line is a genuinely original correction to githubPlan. Data that can silently change the PROTEIN (genetic codes, codon usage, HEG sets) stays label-gated in root `data/`; data that changes WHICH LIABILITIES ARE FLAGGED (motif tables, vendor profiles, MaxEnt matrices) moves in-lane to `bt5/<lane>/data/` with a mandatory `_provenance.json` sidecar and a lane-local integrity test. This dissolves both the `approved:data-change` serialization AND the `data/MANIFEST.sha256` collision that A and B walk straight into.",
     "Invariant I9 \u2014 every immutable interval is byte-identical to the input backbone \u2014 is the highest-value new invariant proposed by anyone. 'The optimizer silently edited the user's backbone' is the worst possible bug in a vector-context tool, and C is the only proposal that makes it a raised exception. A ships `verify_construct` without enumerating it; B's `verify_construct` re-scans motifs but not backbone identity.",
     "`Spec.requires_construct: bool` is a sharp, load-bearing performance insight nobody else has: it lets the 200\u2013500-variant empirical null evaluate cheap specs on the bare ORF and only expensive specs on the assembled plasmid. Without it, critique Tier-1 #3's normalization costs 500 circular-plasmid folds and becomes unshippable \u2014 which is exactly the trap A walks into with its '\u22641 s, no extra cost' claim.",
     "`lattice_terms() -> LatticeTerms | None` letting a rule ship Tier-B-only on day 3 and upgrade itself to Tier-A on day 20 with a lane-3-only diff is the cleanest solver\u22a5rules decoupling of the three, because the upgrade path is explicit rather than implied.",
     "'Never scan two strands' \u2014 close the forbidden-pattern set under reverse complement at construction time and scan forward only \u2014 deletes githubPlan's named #1 agent bug class (`minus_strand_bamhi`) at the type level rather than testing for it.",
     "The windowed-GC analysis (\u00a74.2) is the only honest engineering answer in the three: three options tabulated, the block-state DP costed out (600 \u00d7 52 = 31.2k states \u2192 ~92 M relaxations \u2192 0.5\u20131.5 s vectorized), a recommendation that explicitly concedes windowed GC will be the one hard constraint NOT guaranteed by construction, and a demand that the owner ratify that in week one rather than discover it in week five.",
     "Four-level Evidence (A/B/C/D with D = folklore) plus `last_verified: ClassVar[str]` rendering as a stale-vendor-rule warning chip. Correctly implements brief \u00a76.8 and directly addresses brief \u00a78.7 (Twist changed homopolymer 14\u219230 between 2023 and 2026)."
    ],
    "weaknesses": [
     "Answers the literal question with 'no, not for three weeks.' Wave 1 is THREE agents for days 8\u201320 and steady state is capped at six concurrent open PRs. On the stated criterion \u2014 can 6\u20138 simultaneous sessions build this \u2014 C scores lowest on raw parallelism by design. It is defensible (githubPlan \u00a78.1 says the honest ceiling is 4\u20135) but it is a real schedule cost: ~1 week of owner-only foundation plus 2 weeks at 3 agents is roughly 20 agent-weeks of idle capacity if you actually have 8 sessions available.",
     "WEEK-ONE FILE (wave-1 version): `packages/engine/src/bt5/core/result.py`. L1 solver owns `InfeasibilityCertificate` and `Relaxation`; L8 score owns `ObjectiveScore`, `ScoreCard` and `Conflict` \u2014 and `Conflict.relaxations: tuple[Relaxation, ...]` welds them together in one file that C deliberately leaves PROVISIONALLY UNFROZEN for exactly those two agents. Both will push on it, neither may edit it, and C's stated remedy is an owner-authored PR 'batched at most once per week'. That is a one-week worst-case block on two of three wave-1 lanes, and C's own \u00a78 admits 'a core/ change during wave 1 costs three rebases.'",
     "SECOND WEEK-ONE FILE: `packages/engine/src/bt5/pipeline.py` (OWNER). L1 changes what solve returns, L5 changes what assembly produces, L8 changes what scoring consumes \u2014 all three wave-1 lanes push on the same owner-owned orchestrator, with no stated cadence, no amendment classes, and no unblocking hatch. C has B's problem without B's protocol.",
     "No amendment protocol at all. C says core/ is 'provisionally frozen' and changes are 'owner-authored, batched weekly, announced to all three sessions' \u2014 that is a cadence, not a mechanism. There is no MINOR/MAJOR taxonomy, no additive-with-default fast path, no deprecation shim, no `ext`-style escape hatch, and no backward-compat fixture test. This is the one axis where B is unambiguously better and C needs the graft.",
     "`Evaluation.raw_score` with a global 'lower is always better' sign convention cannot represent a two-sided band without collapsing to |deviation|, which destroys the native unit. Brief \u00a72.C1 wants CAI in a 0.70\u20130.90 BAND, and brief \u00a72.F5 wants 45\u201360% GC per 100 nt with 'show which side is binding per window' \u2014 the exact display C's own `Conflict.binding_spec_id` promises. L8 has to reconstruct the native unit that L3 already threw away. A (`Direction` + `band` + raw in native units) and B (`ObjectiveValue.raw` + `unit` + `direction`) both get this right.",
     "`Spec.citation: ClassVar[str]` \u2014 ONE url per rule. The rules that most need an honest evidence badge are precisely the contested ones resting on two sources with opposite signs: D8b (Ficarelli 2020 positive vs the 2021 lentiviral null, brief \u00a78.6), C9 CPB (Coleman vs Gutman & Hatfield, brief \u00a78.3), G4 in mammalian cells (brief \u00a78.4). A single string makes the badge dishonest exactly where honesty is the product. A and B both use tuples.",
     "The science is deferred. For three weeks the repo contains three rules (AvoidPatterns, GCBand, Homopolymer) and one host. C's G0 gate \u2014 'confirm each needed ZERO additions to Evaluation/Breach' \u2014 is validated against the three EASIEST rules in brief \u00a72. It does not exercise D3 (iterate-to-fixed-point splice removal), B6 (Salis TIR over a 70-nt window crossing into the vector 5\u2032UTR), C4 (harmonization, which needs a native CDS the type system may not carry), or F5 vs E2 (the two-sided conflicting band). G0 as specified will pass and prove very little.",
     "Lane 3 is oversized (~40 rules across `patterns/` and `models/`) and C only names the split point rather than taking it. And once wave 2 starts, C's seven remaining lanes inherit A's problem unsolved: C never says how ~40 registered rules get their weights into the preset JSON, or whether a manifest snapshot file exists.",
     "The foundation is 4\u20136k hand-written lines plus seven gates, all owner-serial, and the plan has no branch for 'G2 fails.' If the coordinate model is wrong at G2, wave 1 slips by however long it takes one person to redesign `Construct.editable` / `codon_map` \u2014 and C offers no parallel work for anyone during that window."
    ]
   }
  ],
  "winner": "C-vertical-slice \u2014 but only when grafted with A's per-rule-file catalog and B's port record/replay + amendment protocol. C wins because it is the only proposal that treats the shared interface as an UNPROVEN HYPOTHESIS and buys evidence before agent #4 starts, and the only one that actually enumerates and pre-registers the shared mutable files (pyproject.toml/uv.lock, benchmarks/tolerances.yaml, benchmarks/panel.json, the data/ split, generated TS ownership) that githubPlan \u00a78.2 and \u00a78.6 name as the \ud83d\udd34 failure modes at 8 agents. A and B both build 8-10 lanes on top of an interface nobody has stress-tested, and both reintroduce a single shared catalog file at the highest-volume lane \u2014 A as a generated snapshot (rules_catalog.json), B as a hand-written frozen module (catalogue.py). Ranking: C 33, B 31, A 29. The honest caveat: C answers 'can 6-8 sessions build this?' with 'not until day 20', so if 8 sessions are genuinely available and idle, run C's Wave 0 + gates while the other five agents build B-style Fake/Recorded port implementations and the corpus, which need no frozen core.",
  "mustGraft": [
   "FROM A \u2014 one file per rule at `bt5/rules/catalog/<brief_id>_<slug>.py`, filenames mapping 1:1 onto brief \u00a72 IDs (d8b_cpg_zap_window.py, f5_at_window_band.py, d3_v5_donor.py). C ships only three rules and names `patterns/` + `models/` as directories; A's naming convention is what makes ~45 rules reviewable, makes 'did we implement D5(c)?' an `ls`, and lets two rules agents share a directory with zero conflicting hunks. Adopt A's convention, but DO NOT adopt A's `rules_catalog.json` snapshot \u2014 serve the catalog from the live registry at request time instead (C's `GET /capabilities`), so no generated file is committed.",
   "FROM A \u2014 `RuleSpec.weight_provenance` as a CI-enforced non-empty string on every SOFT rule, plus a contract test running over EVERY discovered rule asserting: \u22651 citation, folklore \u21d2 default_enabled is False, HARD_LATTICE \u21d2 non-empty motif list, evaluate() is pure (same input twice \u2192 identical result), every Violation interval lies inside the construct. Critique Tier-1 #3's corollary \u2014 'the default weight vector is the product' \u2014 given teeth. A rule agent gets its entire test suite for free and cannot ship a rule missing an evidence badge.",
   "FROM A \u2014 `SegmentKind.ANNOTATED_INTRON` and `WHITELISTED_REPEAT`. Critique Tier-3 #5 is that the splice-removal rule will happily destroy a deliberately placed CMV/\u03b2-globin intron, and brief \u00a73.3 is that LTRs and ITRs irreducibly violate the plasmid-stability rules. C's `Construct.editable` marks what may be MUTATED but has no concept of a region that is immutable AND EXEMPT from a scan. Without this, D3 and F1/F3 fire on every real vector.",
   "FROM A \u2014 `Violation.fixable_by_codon_choice: bool`, routing B10 (out-of-frame uAUG in the user's own 5\u2032UTR) and F7 (TM-segment toxicity) to the protocol/relaxation advisor rather than the solver. C's `Breach` has magnitude and interval but no way to say 'this is real and you cannot fix it by choosing codons', so the solver will chase unfixable breaches into spurious infeasibility.",
   "FROM A \u2014 per-rule cost accounting: re-score each candidate with each hard rule individually relaxed, producing 'enforcing D1 cost you 3 percentile points of 5\u2032 accessibility and changed 11 codons.' This is brief \u00a76 gap #8 and the strongest defensibility feature in a field where vendor optimizers are a coin flip. Cheap once ActiveRule is data.",
   "FROM A \u2014 the x1_oligo_match_loss REPORT_ONLY rule (critique Tier-5 #7: recoding silently invalidates the user's existing qPCR primers, shRNA, and HDR homology arms) and the x2 protein-liability scan on the ASSEMBLED fusion (critique Tier-3 #6: a linker creating an N-X-S/T sequon across the junction). Neither B nor C mentions either; both are guaranteed support tickets.",
   "FROM A \u2014 `RuleSpec.conflicts_with` as a declared edge set on the spec. C derives conflicts purely from two Breaches overlapping an interval, which finds positional conflicts but MISSES the declared structural ones brief \u00a76.7 names: NcoI CCATGG \u2282 Kozak GCCACCATGG, Bxb1 attB/attP containing GGTCTC (BsaI), CpG depletion vs the vendor GC floor. Those must be declared, not discovered.",
   "FROM B \u2014 Real/Recorded/Fake implementations of every port, with fixtures namespaced by CONSUMING lane (`tests/fixtures/ports/<port>/<consumer_lane>/<call_hash>.json`) and `FixtureMissing` naming the exact `--record-port` command. C's skeleton-stub approach gives a lane one degenerate implementation; B's gives it a replayable transcript per call site that two lanes can never collide on. Fix B's own inconsistency by namespacing the HTTP transcripts the same way (`tests/fixtures/http/<route>/<consumer_lane>/<case>.json`).",
   "FROM B \u2014 the nightly `fixture-fidelity` job: re-run every recorded fixture against the REAL implementation, fail non-blocking, open an issue naming port/consumer/hash. Non-blocking is the load-bearing design choice \u2014 a stale fixture must never stall a lane's PR but must never go unnoticed. Neither A nor C has any drift detection on their stubs.",
   "FROM B \u2014 per-lane golden snapshot directories `tests/goldens/<lane>/__snapshots__/` with a per-lane `cases.json`, and extend `goldens-not-hand-edited` to walk all of them. githubPlan ships ONE shared `tests/goldens/__snapshots__/`; at 8 lanes every optimizer-touching PR rewrites it. B is the only proposal that caught this defect in the base plan, and it is a week-one collision in all three architectures.",
   "FROM B \u2014 the amendment protocol, whole (\u00a77.3\u2013\u00a77.6). MINOR (new optional field with a default, new model, new route) on a fast path touching only the contracts package; MAJOR (remove/rename/retype) requiring an RFC plus a deprecation shim with a `model_validator(mode='before')` that fills the new field from the old; a two-window rule keeping the deprecated field until every consuming lane has merged once; and `test_backward_compat` re-parsing every recorded fixture INSIDE the amendment PR to prove nobody broke. C has 'owner batches core changes weekly', which is a cadence, not a mechanism \u2014 grafting this is what bounds C's admitted 'three rebases' cost during Wave 1.",
   "FROM B \u2014 the `ext: dict[str, JsonValue]` lane-namespaced extension bag on every cross-lane model, plus the nightly `ext-debt` job that opens one issue per key older than 14 days. Adopt it with B's limitation stated honestly in CLAUDE.md: ext unblocks the INTRA-lane discovery only (M5 finds FoldEnergy needs `dangles`); a cross-lane need still requires a real amendment, so the fast path must actually be fast.",
   "FROM B \u2014 `ProteinInput.translation_table_id` with NO DEFAULT so the model cannot be constructed without it, plus `HostProfile.locked_translation_table_id` and a validator asserting the two agree. Strictly stronger than C's prose 'EXPLICIT. Never defaulted silently.' Pair with C's property test `translate(back_translate(p, t), t) == p` over all 30+ NCBI tables and A's per-SEGMENT `table_id` for organelle-targeted fusions.",
   "FROM B \u2014 `FoldEnergy` as a value type carrying dg_kcal_mol + engine + engine_version + param_set + temperature_c + dangles, so a \u0394G number cannot travel without its provenance. Combine with A's `engine_calibration` key on the spec, which refuses to APPLY a threshold when the active engine differs and degrades to reporting the metric. Brief \u00a77-tech-iii names silently applying a ViennaRNA-calibrated \u221239 kcal/mol to seqfold output as the most likely correctness bug in the whole product; C's `FoldEngine.params` ClassVar records the engine but nothing gates the threshold on it.",
   "FROM B \u2014 `tools/mockserver` replaying recorded HTTP transcripts and validating BOTH request and response against the frozen OpenAPI, with the SAME transcripts asserted against the real ASGI app via `httpx.ASGITransport` in the server lane. Consumer-driven contract testing means neither the server nor the web lane can drift without one of them going red, and neither waits for the other to exist. C's `tests/fixtures/api/*.json` is one-directional and only unblocks the web lane.",
   "FROM B \u2014 `contract-freeze` as a required CI job that regenerates the JSON schema, the OpenAPI document and the TS client and fails on any diff without an `approved:contract-*` label, plus a check that the contract version incremented. This is the enforcement C's 'core/ freezes for real when wave 1 lands' currently lacks entirely.",
   "FROM B \u2014 explicit `changes:` path filters for EVERY lane, present in PR #0. githubPlan's filters are coarse (`python` covers packages/engine + packages/server + data + benchmarks), so today every engine lane's PR runs every other lane's tests, invariants and benchmark-gate. At 8 lanes against 20 job slots that is both the concurrency starvation of \u00a78.1 and the cross-lane red-CI interference of \u00a78.5. `.github/**` is label-gated, so if this is not in PR #0 it costs an owner PR later.",
   "FROM B \u2014 the `ReferenceSolver`: a deliberately dumb, obviously-correct solver (exhaustive over \u226412-codon windows, greedy elsewhere) shipped in the foundation. It unblocks every rules lane from minute one AND survives forever as the differential-testing oracle for the real solver, so the effort is not throwaway. C's skeleton has a real Tier-A DP instead, which is faster but gives the rules lanes nothing to differentially test against."
  ],
  "fatalFlaws": [
   "A \u2014 `packages/engine/rules_catalog.json` (\u00a73.3) is the shared mutable list A claims to have abolished, reintroduced as a CI-diff-gated generated file. RULES-SCAN and RULES-MODEL \u2014 A's own recommended split to reach 10 lanes \u2014 both regenerate it on every single PR, entries sort by rule id so hunks interleave, and every merge invalidates every other open rules PR. The `rules-catalog-snapshot` job turns a mergeable conflict into a hard CI failure. This breaks A's central parallelism claim at the highest-volume lane. Fix: serve the catalog from the live registry at request time; commit nothing.",
   "B \u2014 `packages/contracts/src/bt5_contracts/catalogue.py`: ~70 RuleSpecs hand-transcribed from brief \u00a72 into one owner-owned frozen file before any rule is implemented, with `RuleKind` (which determines solver tier), `objective_id`, `hosts`, `delivery` and `conflicts_with` fixed per rule. Both rules lanes need it in week one and neither may edit it, and the block is HARD \u2014 'an implementation must match an existing spec id', so a rule with a wrong spec cannot ship at all. B's own \u00a710 admits `RuleKind` is probably wrong for windowed GC. 'Zero blocked minutes' is false for M2 and M3.",
   "B \u2014 the `ext` escape hatch cannot unblock the case that blocks people. It is namespaced per WRITING lane and 'no lane may read another lane's namespace' (contract-test enforced), so it works only when producer and consumer are the same lane. Both of B's worked examples are intra-lane. Lane X needing a field lane Y produces \u2014 the actual blocking scenario \u2014 is exactly what ext forbids.",
   "ALL THREE \u2014 the wiring file is owner-owned and nobody names who edits it or when. A's `pipeline.py` + `Deps`, B's `bt5/api.py` FROZEN facade with stub bodies returning recorded data, C's `pipeline.py` (OWNER). Each lane's transition from stub to real implementation requires an edit to that one file. None of the three specifies an owner, a cadence, or a mechanism. Entry-point-based Deps resolution (each lane registers its own implementation, pipeline resolves by protocol) would eliminate it entirely; as written it is a guaranteed owner-serialized bottleneck the moment lanes start landing real work.",
   "ALL THREE, mitigated only by C \u2014 `pyproject.toml` / `uv.lock`. githubPlan \u00a78.2 flags it \ud83d\udd34 as the highest-churn, least-mergeable file in the repo, and CLAUDE.md \u00a75 forbids hand-resolving it. In week one the solver lane needs pyahocorasick, the structure lane needs seqfold + viennarna, the assembly lane needs tatapov + primer3-py, the vector lane needs biopython + a SnapGene reader, and the rules lane needs numpy/scipy for MaxEnt and the Salis TIR model. A and B never mention dependency management once. Only C pre-declares every dependency, extras included, in PR #0. This is non-negotiable in the final plan.",
   "ALL THREE, mitigated only by C \u2014 `data/**` is label-gated `approved:data-change` in githubPlan and carries a single `data/MANIFEST.sha256` listing one hash per asset. A routes codon tables, PaxDb HEG sets, tRNA GCN, `data/vendors/*.toml` and `data/elements/*.json` through it; B routes codon tables, the motif DB and vendor profiles through it. Four lanes need reference data in week one, so four PRs are owner-serialized AND collide on one manifest file. Only C splits the line: protein-changing data stays gated in root `data/`, liability-flagging data moves in-lane with a `_provenance.json` sidecar and a lane-local integrity test.",
   "ALL THREE \u2014 SCIENTIFICALLY WRONG: none models brief \u00a72.D3's explicit requirement that splice-site removal ITERATE TO A FIXED POINT. The brief states plainly that point-mutating one donor activates cryptic donors nearby (the A2UCOE case) and that 'a single-pass remove-motifs step is unsafe.' A declares splice rules ordinary `LocalizationPolicy.MOTIF_LEN_MINUS_1` localized repairs; B assigns them `RuleKind.WINDOW`/`GLOBAL` with single-pass Tier-B repair; C's `localize()` + Tier-B has no convergence contract. All three will ship a construct whose donors were 'removed' into new donors, and the independent validator will pass it because the specific 9-mer it was told to avoid is gone. This needs an explicit re-scan-to-convergence contract with an iteration cap and a documented failure mode.",
   "A \u2014 SCIENTIFICALLY WRONG COST MODEL: \u00a79.3 and \u00a712 claim the 200\u2013500-variant empirical null costs '\u22641 s' and 'no extra cost' because it reuses variants 'already sampled.' But A deliberately admits no bare-string evaluator \u2014 every rule takes a `ConstructRecord` \u2014 so the null is 400 variants \u00d7 every active rule on the assembled circular plasmid, Tier-C folding rules included. That is minutes, not one second. Critique Tier-1 #3's normalization, the mechanism the entire slider UI rests on, becomes unshippable. C is the only proposal that spots this and it needs both a dedicated flag (`Spec.requires_construct`, cheap rules on the bare ORF) and a measured gate (G3: <2 s or normalization leaves the interactive path). Graft both.",
   "C \u2014 `Evaluation.raw_score` with a global 'lower is always better' sign convention cannot represent a two-sided band without collapsing to |deviation|, destroying the native unit. Brief \u00a72.C1 wants CAI in a 0.70\u20130.90 band and \u00a72.F5 wants 45\u201360% GC per 100 nt with 'show which side is binding per window' \u2014 the exact display C's own `Conflict.binding_spec_id` promises and brief \u00a73.3 demands. The scoring lane has to reconstruct a unit the rules lane already threw away. Replace with A/B's shape: raw in native units + an explicit `unit` string + a `Direction` enum including BAND.",
   "C \u2014 `Spec.citation: ClassVar[str]`, one URL per rule. The rules that most need an honest evidence badge are the contested ones resting on two sources with OPPOSITE signs: D8b ZAP (Ficarelli 2020 vs the 2021 lentiviral null, brief \u00a78.6), C9 codon-pair bias (Coleman vs Gutman & Hatfield, brief \u00a78.3), G4 in mammalian cells (reporter assays vs in-cell probing, brief \u00a78.4). A single string makes the badge dishonest precisely where honesty is the differentiator. Use a tuple of citations with label/url/year/verified-date, as A and B both do.",
   "A \u2014 `Evidence` collapses vendor-assertion and folklore into one value (`C = 'vendor_or_folklore'`) and then CI-asserts `Evidence.C \u21d2 default_enabled is False`. Brief \u00a76.8 and critique Tier-2 #8 want three distinct badges with only FOLKLORE defaulting off. A's collapse ships every vendor-asserted manufacturability rule \u2014 the ones Twist and IDT actually enforce at order time \u2014 disabled by default, which is the opposite of what the user needs. Use C's four levels.",
   "B \u2014 three committed generated artifacts (`packages/contracts/schema/v1/openapi.json`, `packages/contracts-ts/src/generated.ts`, `apps/web/src/api/generated/`), all rewritten by every amendment and all diff-gated by `contract-freeze` as a hard fail. Every MINOR amendment \u2014 which B expects to be frequent and cheap \u2014 simultaneously red-lights every open server and web PR. The amendment path is fast for the owner and expensive for everyone downstream, which inverts B's own design goal.",
   "A \u2014 no CI-capacity analysis whatsoever. A proposes 8 lanes 'scaling to 10' against githubPlan \u00a78.1's measured ceiling (20 concurrent job slots, ~12 consumed per Python PR, honest answer 4\u20135 sessions) and then ADDS three CI jobs on top (`rules-catalog-snapshot`, `openapi-drift`, nightly `vendor-oracle`). B and C both do the arithmetic and cap accordingly; A never acknowledges the constraint exists, which makes its lane count aspirational rather than planned.",
   "C \u2014 G0, the gate on which the entire framing rests ('implement the three skeleton rules against `Spec` and confirm each needed ZERO additions to Evaluation/Breach'), is validated against AvoidPatterns, GCBand and Homopolymer \u2014 the three easiest rules in brief \u00a72. It does not exercise D3 (iterate-to-fixed-point), B6 (Salis TIR over a 70-nt window that crosses into the vector's 5\u2032UTR), C4 (harmonization, which needs a native CDS the type system may not carry at all), or the F5-vs-E2 two-sided conflicting band. G0 as specified will pass and prove almost nothing. It must be re-scoped to one rule from each of the four hardest families before the freeze is treated as real."
  ]
 },
 {
  "scores": [
   {
    "proposal": "C-vertical-slice",
    "total": 31,
    "scientificCorrectness": 8,
    "parallelBuildability": 6,
    "riskManagement": 9,
    "concreteness": 8,
    "strengths": [
     "I9 ('every immutable interval is byte-identical to the input backbone') is the single highest-value invariant proposed by anyone. Neither A nor B can detect 'the optimizer silently edited the user's vector' \u2014 A has a BACKBONE_IMMUTABLE SegmentKind and B has an `editable` tuple, but neither validates the bytes afterward. I10 (cassette frame across the assembled cassette, brief \u00a72.A4) is likewise unique.",
     "The only correct model of brief \u00a73.3. A Spec is instantiated once PER GATING SLOT, so `Breach.slot_role` always names which of propagation/packaging/target fired it, and two slots demanding opposite things produce two Breaches over the same Interval \u2014 which is exactly the input the conflict detector needs. A carries `ActiveRule.role` but flattens rules into one set; B has three optional DesignContext fields and a `Finding` type with no role attribution at all.",
     "`DesignContext.cassette_orientation` and `ContextSlot.strand_of_interest` are REQUIRED inputs. Brief \u00a73.3: 'Cassette orientation is a required input; without it the polyA/splice analysis is exactly backwards.' C is the only proposal that makes this un-defaultable.",
     "Closing the forbidden-pattern set under reverse complement at construction and scanning only the forward strand deletes githubPlan's #1 named agent bug class (`minus_strand_bamhi`) at the type level rather than testing for it. A leaves both-strand scanning to ~45 independent rule authors via a static `RuleSpec.strands` field.",
     "Seven numeric go/no-go gates measured against a working product BEFORE agent #4 starts, each with a named fallback action. G4 (\u03bb-sweep gallery collapse to near-identical sequences) is a real product-invalidating risk that brief \u00a74 explicitly warns about \u2014 'weighted-sum sweeps recover only supported (convex-hull) Pareto points' \u2014 and neither A nor B mentions it once.",
     "The only honest treatment of brief \u00a77-tech-i. C tabulates three windowed-GC options with costs, recommends the weaker one (Lagrangian steering + Tier-B repair + independent validator), states plainly that 'windowed GC is the one hard constraint not guaranteed by construction,' and asks the owner to ratify it in week one. B assumes the problem away inside `RuleKind.WINDOW`.",
     "G3's pass criterion embeds a negative control \u2014 the optimized design must reach >0.90 percentile on its own objective and \u22480.50 on an objective it ignored. That is critique Tier-7 Layer 4 wired into a week-one gate rather than deferred to a benchmark someone will own later.",
     "`KmerIndex.of()` takes a Construct and nothing else; there is no constructor accepting an external database, plus a CI grep. Structural, not policy."
    ],
    "weaknesses": [
     "FATAL-ADJACENT: `Spec.requires_construct: ClassVar[bool]` with 'False == cheap, evaluable on the bare ORF' is a documented, sanctioned hole in the product's central differentiator. Any rule an agent marks False will miss junction-spanning and origin-spanning hits \u2014 precisely the E1 homopolymer / D1 site / F1 repeat cases brief \u00a76.1 says every competitor gets wrong. Worse, it is a ClassVar, so the same rule cannot be cheap-in-the-null and full-in-the-final-score. C's own G2 gate tests exactly this failure mode on three rules, then hands lane 3 a switch to opt ~40 rules out of it for speed.",
     "No `unit` field anywhere, and `Evaluation.raw_score` carries a forced global 'lower is always better' sign convention. This makes band objectives inexpressible \u2014 CAI 0.70\u20130.90 (C1), global GC 40\u201360% (E2), and the F5 AT-window 45\u201360% GC are all two-sided bands, and a monotone 'lower is better' scalar under a weighted sum pushes every one of them to an extreme. Driving CAI toward 1.0 is the exact failure brief \u00a71.1 spends the whole section refuting.",
     "No `native_cds` anywhere in the type system. `Spec.evaluate(c, ctx, svc)` cannot see it and `DesignContext` does not carry it. Harmonization (C4) is therefore unrepresentable \u2014 and brief \u00a73.1 makes harmonize/native the DEFAULT mode for CHO/HEK, insect and plant. Critique Tier-3 #10 flags harmonization-without-a-native-CDS as an unimplementable-as-specified mode; C reintroduces the same gap silently. B requires `native_cds` to enable the mode; A has `EvalContext.native_cds` and a `requires_native_cds` context gate.",
     "No value-level \u0394G provenance. `FoldEngine.params` is a ClassVar string on the engine, not a field on the number. Once a \u0394G is inside an `ObjectiveScore`, nothing records which engine, parameter set, temperature or dangles model produced it \u2014 brief \u00a77-tech-iii calls this 'the most likely correctness bug in the whole feature.' G6 measures the offset once; it does not stop a mis-attributed number from flowing into a report.",
     "G0, the gate for C's own self-declared kill-risk R1, is the weakest gate in the set. Passing it means three rules \u2014 AvoidPatterns, GCBand, Homopolymer \u2014 needed zero additions to `Evaluation`/`Breach`. None is model-based (MaxEntScan, Salis TIR, promoter calculator, AREScore), none is gated across three slots with opposing demands, none needs a native CDS, none needs an engine-gated threshold. G0 will pass trivially while leaving every real source of interface pressure untested.",
     "`Relaxation.predicted_cost` = 'delta percentile on EVERY other objective' requires re-solving under each relaxed constraint. C never budgets it and G7's 10 s end-to-end ceiling has no room for N re-solves. (A promises the identical feature with an equally absent budget.)",
     "Seven of ten lanes idle for roughly two weeks. C is honest that this is deliberate and grounds its concurrency ceiling in githubPlan \u00a78.1's actual arithmetic (20 job slots, ~12 per Python PR), which A and B both assert past \u2014 but it is still the lowest raw parallelism of the three."
    ]
   },
   {
    "proposal": "A-domain-layered",
    "total": 30,
    "scientificCorrectness": 7,
    "parallelBuildability": 9,
    "riskManagement": 5,
    "concreteness": 9,
    "strengths": [
     "The `Enforcement` enum (HARD_LATTICE / HARD_REPAIR / HARD_CHECK / SOFT / REPORT_ONLY) with a pipeline assertion that a HARD_LATTICE rule cannot be routed to the weighted sum is the only mechanism in any of the three proposals that structurally enforces the brief's cardinal prohibition: 'hard constraints must be guaranteed by construction or by reject-and-repair + independent validation, never by penalty weights.' B has one undifferentiated `HARD`; C has `Severity.HARD` with the routing decided by whether `lattice_terms()` happens to return None.",
     "`RuleSpec.engine_calibration` is refused at load time when the active FoldEngine reports a different (name, params), and the app degrades to REPORTING the metric without the threshold. That is the honest response to brief \u00a77-tech-iii \u2014 strictly safer than C's plan to transfer Bo\u00ebl's \u221239 kcal/mol via a measured constant offset. B carries engine identity in the value but never says what happens on mismatch.",
     "`HARD_CHECK` ('pre-flight only; NOT fixable by codon choice') plus `Violation.fixable_by_codon_choice` correctly models B10 uORFs in the user's own 5\u2032UTR \u2014 a constraint the optimizer must report and must not pretend to fix.",
     "`LocalizationPolicy` turns DnaChisel's imperative localization heuristics into data the solver consumes generically, which is what actually decouples ~45 rule files from the solver lane.",
     "The rule-family \u2192 file map covers all of brief \u00a72 with 1:1 ID correspondence (d8b_cpg_zap_window.py, d5_antisense_tataat.py, c10_oof_stop_density.py, x1_oligo_match_loss.py). Verifying 'did we implement D5(c)?' is an `ls`. Highest scientific coverage fidelity of the three.",
     "The worked D8b file is correct science: it gates on density AND mean spacing, reports the worst window rather than a global count, and its `weight_provenance` quotes Ficarelli's 'magnitude was NOT correlated with the number of CpGs' and brief \u00a78.6's 2021 lentiviral null result. This is the only place any proposal demonstrates what a defensible default weight looks like.",
     "`weight_provenance` as a CI-enforced non-empty prose field on every SOFT rule is the strongest answer to critique Tier-1 #3's corollary ('the default weight vector is the product'). B requires only a citation URL on a preset; C requires a `rationale` field but only on presets, not per rule."
    ],
    "weaknesses": [
     "The packaged strand is never plumbed into evaluation. `ConstructRecord.packaged_strand` and `strand_view()` exist, but `EvalContext` has no strand field and `RuleSpec.strands` is a STATIC tuple frozen in the rule file \u2014 the worked example ships `strands=(1,)`. For a reverse-oriented lentiviral cassette (brief \u00a73.3, \u00a72.G) the packaged genome is the minus strand, so D4 `AATAAA` hard-fail and D3 splice-donor scanning run on the wrong strand and report clean. Brief: 'without it the polyA/splice analysis is exactly backwards.' This is a shipped-wrong-answer path in the flagship use case.",
     "`Evidence` has only A/B/C, with C = 'vendor_or_folklore', and the rule contract test asserts `evidence == C \u21d2 default_enabled is False`. Brief \u00a76.8 requires three distinct badges (EVIDENCE-BACKED / VENDOR-ASSERTED / FOLKLORE) with only FOLKLORE default-off. A's collapse mechanically forces every vendor-asserted manufacturability rule default-OFF \u2014 and brief \u00a72.E rates homopolymer limits, the 50 bp GC window, direct-repeat \u226520 bp and k-mer uniqueness as evidence A precisely because they rest on 1,076 real vendor outcomes. B and C both ship four levels correctly.",
     "`Segment.table_id` is per-segment. A single ORF is translated by one ribosome under one genetic code; two DESIGNABLE_CDS segments in the same reading frame with different `table_id` values is biologically impossible, and A states no invariant forbidding it. Because `verify_construct` re-derives translation per segment, a mis-set segment table produces a wrong protein that PASSES verification. A footgun introduced in the frozen core to serve a case (organelle-targeted fusion) that per-host locking already covers.",
     "`NullModel` is built from 'random synonymous variants of THIS protein' but rules declare `scope` = cassette or construct (the worked D8b is scope='cassette'). Percentiles for construct-scoped rules \u2014 GC windows crossing the origin, F1 repeats shared with the backbone, junction homopolymers \u2014 are computed against a null distribution that never contains backbone context. The reported percentile is then not the quantity the report claims it is.",
     "The performance budget is wrong by an order of magnitude where it matters most. \u00a712 allots \u22641 s for a 400-variant null while allotting \u22641 s for ONE full fold. The null must cover every active rule including B1 (the \u22124..+37 \u0394G window), which is the highest-weighted bacterial objective. Either the null silently omits the most important term or the interactive budget is fiction. C caught this exact risk (R6) and gated it (G3); A did not.",
     "`verify_construct(c.record, prob.rules)` re-runs the same `rule.evaluate()` functions the solver used. For the A1\u2013A3 invariants A inherits githubPlan's independent re-derivation, but every rule-level hard constraint (D1 sites, F1/F3 repeats, G size caps) is 'validated' by the code that produced the sequence. critique Tier-7 Layer 1 requires 'an independent validator written against a different code path than the optimizer's scorer.'",
     "'The list is closed at four' localization policies is asserted, not argued, and it is already wrong: brief \u00a72.D3 requires iterate-to-fixed-point splice repair ('point-mutating one donor activates cryptic donors nearby\u2026 a single-pass remove-motifs step is unsafe'), which none of MOTIF_LEN_MINUS_1 / WINDOW_MINUS_1 / WHOLE_SCOPE / PAIRED_SEGMENTS expresses.",
     "No measurements anywhere. \u00a714 lists four honest weaknesses and mitigates all four by assertion. Windowed GC is declared HARD_REPAIR without measuring the block-state DP alternative; gallery degeneracy is not mentioned; the null-model cost is not measured; the seqfold/ViennaRNA offset is handled by a load-time refusal that was never tested against real numbers."
    ]
   },
   {
    "proposal": "B-contract-first",
    "total": 28,
    "scientificCorrectness": 6,
    "parallelBuildability": 9,
    "riskManagement": 5,
    "concreteness": 8,
    "strengths": [
     "`SequenceView` with `left_flank_automaton_state: int` and `right_flank: str` is the sharpest expression of brief \u00a74's Tier-A junction guarantee in any of the three: seed the Aho\u2013Corasick automaton with the state after consuming the immutable left flank, require a non-accepting state after the right flank. A and C both describe junction-spanning evaluation; only B puts the automaton seed state in the type a rule receives.",
     "`FoldEnergy` as a value type carrying dg, engine, engine_version, param_set, temperature_c and dangles is the correct fix for brief \u00a77-tech-iii \u2014 a \u0394G cannot travel through the system stripped of the engine that produced it. Neither A nor C attaches provenance to the number itself.",
     "`ProteinInput.translation_table_id` with NO default, so Pydantic refuses to construct the model, plus `HostProfile.locked_translation_table_id` and a cross-validator. critique Tier-1 #2 becomes structurally impossible rather than a thing someone must remember. The best treatment of the CTG=Ser / TGA=Trp class of silent protein-changing bugs.",
     "`DesignRequest.screen_token: ScreenToken` is a required, non-defaulted HMAC over (protein_sha256, verdict, db_version), verified before anything runs. The strongest biosecurity gate of the three \u2014 A's `NullScreen` returns 'not_run' and the pipeline proceeds anyway; C shows a banner.",
     "`RepeatBlock` computed in the FROZEN protein layer before the lattice is built, with `max_nt_identity_allowed: float = 0.85`, enforced as `Enforcement.HARD`. Correctly implements critique Tier-1 #4's demand that repeat-breaking OVERRIDE codon optimality rather than compete with it.",
     "`ObjectiveValue.percentile` and `.normalized` required and non-defaulted, with `raw` banned from solver/, server/ and apps/web/ by CI grep and contract test. The cleanest structural answer to critique Tier-1 #3.",
     "Genuinely the best parallel-build machinery: zero import-time edges between M1\u2026M7, entry-point registries, Real/Recorded/Fake implementations of every port, per-lane fixture namespacing (`fixtures/ports/folding/solver/` vs `.../rules_expression/`), per-lane golden directories (which correctly identifies that one shared `__snapshots__` is a nine-way conflict storm), consumer-driven HTTP transcripts replayed from both sides, and an amendment protocol with a bounded escape hatch."
    ],
    "weaknesses": [
     "FATAL: 'Called by the solver, by the independent validator, and by the report writer \u2014 via three different call paths, which is what makes \"independent final validator\" real.' This is false, and it is stated as a safety guarantee. Three callers of the same pure function give zero independence against a wrong predicate \u2014 if `f1_direct_repeats.scan()` has an off-by-one in its MEPS threshold, all three call sites agree and the plasmid ships. critique Tier-7 L1 demands a validator 'written against a different code path than the optimizer's scorer.' B's `verify_construct()` covers the A1\u2013A3 invariants; every rule-level hard constraint is self-certified.",
     "`Enforcement.HARD` is a single undifferentiated value. Nothing in B's type system distinguishes 'guaranteed by Tier-A domain restriction' from 'attempted by Tier-B repair,' and `Rule.hints()` returning None silently demotes a HARD rule to repair with no assertion that repair converged. `RuleKind` routes to a solver tier but is a mechanism hint, not an enforcement guarantee \u2014 there is no equivalent of A's assertion that a hard rule can never enter the weighted sum. This is the brief's one non-negotiable prohibition and B has no mechanism for it.",
     "`RuleSpec.enforcement` is frozen once per rule id in a central catalogue, so D4 cannot be HARD on the lentiviral packaged sense strand and SOFT elsewhere \u2014 which is exactly what brief \u00a72.D4 and \u00a73.2 specify ('H for lentiviral sense strand, S elsewhere'). Same for D3 (HARD for lentiviral/genome-integrated, warn for plasmid) and D6 G4 (hard for bacterial CDS, soft for mammalian mRNA, brief \u00a78.4). B's three-way context is three optional DesignContext fields that 'independently populate the rule set,' but a rule appears once, at one enforcement level.",
     "`Finding` has no role/slot field. When the packaging-cell context and the target-cell context both fire on the same interval demanding opposite things \u2014 the entire point of brief \u00a73.3 \u2014 B cannot tell the user which context produced which finding. `RuleConflict.binding_side` names a rule id, not a context. The conflict panel that is supposed to be the honest output of the compound case is under-determined by the data model.",
     "`ScoreVector.total = \u03a3 w\u1d62 \u00b7 normalized_i` over direction-corrected normalized values, with `Direction.BAND` declared in the enum and never defined for normalization. CAI (C1), global GC (E2) and the F5 AT-window are all two-sided bands; a monotone weighted sum drives every one to an extreme. Maximizing CAI is the specific outcome brief \u00a71.1 exists to prevent, and the app would do it while displaying an honest-looking percentile.",
     "Default weight provenance is `ApplicationPreset.citations: tuple[HttpUrl, ...]` required non-empty. A URL is not provenance for a MAGNITUDE. critique Tier-1 #3's corollary is that the default weight vector IS the product; a preset that cites Kudla 2009 and sets w=0.35 passes B's CI check while explaining nothing about why 0.35. A's prose `weight_provenance` field and C's `rationale` field are both materially better.",
     "The ~70-entry RuleSpec catalogue is frozen at PR #0, served at `GET /v1/rules`, and 'the UI can render the full rule list on day one,' with 'a spec with no implementation is a legal (reported) TODO.' Nothing in the RuleSpec type or the route marks implemented-vs-TODO, so the UI ships toggles and evidence badges for rules that are no-ops, and the report implies enforcement that did not occur.",
     "`Recorded` fixtures are the DEFAULT in unit tests and `FakeFoldingBackend` returns dg = \u22120.031 \u00d7 GC-count. Every structure rule in M2 \u2014 B1 (\u22124..+37), B3 (the Bo\u00ebl \u221239 kcal/mol AND GC(2\u20136)>62% dual gate), B11 (\u221230/\u221250/\u221260 cap-proximal) \u2014 would be authored and merged against a linear function of GC content. `fixture-fidelity` is explicitly nightly and non-blocking, so thresholds calibrated against a fake energy model merge on green CI.",
     "The `ext` escape hatch is self-refuting on exactly the case B chose to illustrate it (\u00a77.4): M5 discovers `FoldEnergy` is dishonest without dangles and temperature, and B's prescribed response is to keep shipping \u0394G values whose dangles and temperature are recorded in a namespace no other lane is permitted to read. For an interface defect that is a SCIENTIFIC defect, 'keep working and file an amendment' means knowingly emitting mis-provenanced numbers for a day.",
     "No measurements and no gates. B concedes in \u00a710.1 that a fifth `RuleKind` for windowed GC would be a MAJOR amendment hitting M1, M2 and M3 simultaneously, and mitigates with a spike report due 48 hours AFTER all nine lanes have started writing rule files against the four-value taxonomy."
    ]
   }
  ],
  "winner": "C-vertical-slice",
  "mustGraft": [
   "From A: the `Enforcement` enum separating HARD_LATTICE / HARD_REPAIR / HARD_CHECK / SOFT / REPORT_ONLY, with the pipeline assertion that a HARD_LATTICE rule can never be routed into the weighted sum. This is the only mechanism proposed by anyone that structurally enforces the brief's rule that hard constraints are guaranteed by construction or by repair-plus-independent-validation, never by penalty weight. C's `Severity.HARD` + optional `lattice_terms()` cannot express the difference and must be replaced.",
   "From A: `RuleSpec.engine_calibration` with load-time refusal \u2014 a \u0394G threshold declared as calibrated against 'viennarna:rna_turner2004' is REFUSED when the active FoldEngine reports different (name, params), and the app degrades to reporting the metric with no threshold. Adopt this in preference to C's G6 constant-offset transfer, and keep G6 as the measurement that tells you whether an offset-corrected threshold is even defensible.",
   "From B: `FoldEnergy` as a value type (dg_kcal_mol, engine, engine_version, param_set, temperature_c, dangles). C has no `unit` field at all and carries engine identity only as a ClassVar on the engine object; once a number reaches `ObjectiveScore` its provenance is gone. Brief \u00a77-tech-iii names this the most likely correctness bug in the feature.",
   "From B: `SequenceView.left_flank_automaton_state` and `.right_flank` \u2014 seed the Aho\u2013Corasick automaton with the state after consuming the immutable left flank and require a non-accepting state after the right flank. This is what makes junction-spanning motif exclusion a Tier-A GUARANTEE rather than a post-hoc scan.",
   "From B: `translation_table_id` as a required field with NO default that the model layer refuses to construct without, plus a host-level `locked_translation_table_id` and a cross-validator. C makes `ContextSlot.table_id` required, which is good, but B's 'a model with no default cannot be silently wrong' framing plus the property test over all 30+ NCBI tables should be adopted verbatim.",
   "From B: `DesignRequest.screen_token` as a required, non-defaulted field verified against sha256(protein) before anything runs. C's 'screen_protein() is called before the mutation space is built' is an ordering convention an agent can reorder; B's is a type error. Add the missing piece B lacks: refuse to proceed on verdict='block', not merely on a missing token.",
   "From A: `RuleSpec.weight_provenance` as a CI-enforced non-empty PROSE field on every soft rule, and A's worked D8b string as the reference standard (it cites Ficarelli, states that inhibition magnitude is not correlated with total CpG count, and explains why the weight is moderate given brief \u00a78.6's 2021 null result). C's preset-level `rationale` is too coarse; B's citation URL is not provenance for a magnitude.",
   "From A: four-file separation of the D8 CpG metrics (TLR9 hexamers, ZAP density-AND-spacing window, CpG islands) and A's worked ZAP evaluator, which correctly gates on density AND mean spacing and reports the worst window rather than a global count. Brief \u00a72.D8: 'do not collapse into one slider.'",
   "From A: `Violation.fixable_by_codon_choice` and the HARD_CHECK enforcement class, so B10 uAUGs in the user's own 5'UTR are reported as un-fixable rather than silently attempted or silently dropped.",
   "From A and B: filesystem/entry-point rule discovery with NO shared registry list, and B's per-lane golden and fixture namespacing (`tests/goldens/<lane>/__snapshots__/`, `fixtures/ports/<port>/<consumer>/`). C already has autodiscovery; it does not have B's per-lane snapshot split, and one shared `__snapshots__` directory across ten lanes is a guaranteed conflict storm under githubPlan's `goldens-not-hand-edited` job.",
   "From A: the 1:1 brief-\u00a72-ID-to-filename rule catalogue map (a1_\u2026, d5_antisense_tataat, c10_oof_stop_density, x1_oligo_match_loss). C's `rules/patterns/` + `rules/models/` split is too coarse to answer 'did we implement D5(c)?' by inspection, and coverage auditing against a 70-rule brief is a real scientific-completeness control.",
   "From B: `ProteinInput.native_cds`, REQUIRED to enable harmonize mode, greyed out otherwise. C has no `native_cds` anywhere, which makes harmonization \u2014 the DEFAULT mode for CHO/HEK, insect and plant per brief \u00a73.1 \u2014 unrepresentable, and reproduces exactly the gap critique Tier-3 #10 identified.",
   "From A and B: an explicit `Direction`/`band` declaration per objective and a band-aware normalizer. C's forced 'lower is always better' `raw_score` cannot represent CAI 0.70\u20130.90, global GC 40\u201360%, or F5's two-sided 45\u201360% AT-window, and a monotone weighted sum over them drives CAI toward 1.0 \u2014 the precise failure brief \u00a71.1 exists to refute."
  ],
  "fatalFlaws": [
   "ALL THREE inherit githubPlan's benchmark gate unexamined, and that gate is scientifically backwards. `benchmarks/tolerances.yaml` ships `cai: direction: higher_is_better, blocking: true` and `cai_heldout: higher_is_better, blocking: true`, and `tests/invariants/test_round_trip.py` ships `test_cai_beats_random_backtranslation_on_every_seed` asserting `fmean(opt) >= 0.80`. Brief \u00a71.1 establishes that CAI has no predictive value for expression (Kudla r=0.14 n.s.; Welch: 'CAI has no value in predicting gene expression'; Pichia trastuzumab coefficient \u22120.81) and \u00a72.C1 says CAI must be a BAND, never maximized. These are BLOCKING required checks. Eight agents optimizing to green CI will be trained, by the merge gate itself, to maximize the one metric the brief spends its opening section refuting \u2014 and any PR that correctly moves CAI into a band will read as a regression and fail. A says only 'tolerances.yaml extended by the owner'; B assigns it to M4; C pre-registers metric names but not directions. None of the three noticed. This must be fixed in PR #0 by changing `cai` to `direction: band` and deleting or inverting the CAI-beats-random invariant, before any lane starts.",
   "ALL THREE fail brief \u00a72.D3's fixed-point requirement for splice-site removal: 'Iterate to a fixed point: point-mutating one donor activates cryptic donors nearby (A2UCOE). A single-pass remove-motifs step is unsafe.' A closes its `LocalizationPolicy` list at four with no fixed-point policy and asserts the list is closed. B's `RuleKind.GLOBAL` is 'whole-sequence, localized repair' with no convergence contract. C's `Spec.localize(iv)` returns a single extended interval and the Tier-B loop is generic. Consequence: BT5 removes a MaxEntScan donor above 6 bits, creates a stronger one two codons away, and reports 'splice donors removed' \u2014 a biologically wrong result in the lentiviral and genome-integrated contexts where brief \u00a73.2 marks cryptic donors HARD for titer and safety.",
   "ALL THREE compute the empirical null in a different context from the score it normalizes, which makes every reported percentile a different quantity from the one claimed. A builds the null from 'synonymous variants of THIS protein' while its rules declare scope='cassette'/'construct' (its own worked D8b is scope='cassette'). B's `EmpiricalNull` is likewise protein-level while `Rule.scan` always receives the assembled `SequenceView`. C is the worst: `requires_construct=False` explicitly evaluates cheap specs' nulls on the bare ORF. For any rule sensitive to the backbone \u2014 GC windows crossing the origin, F1 direct repeats shared with LTRs/WPRE, junction homopolymers, E8 k-mer uniqueness across the whole construct \u2014 the reported '94th percentile' is computed against a distribution that never contained a backbone. The honest report line that critique Tier-1 #3 asks for becomes false.",
   "ALL THREE route rule-level hard constraints through the same evaluator the optimizer used, so 'independent final validator' is only true for the A1\u2013A3 translation invariants. B is worst because it makes the claim explicitly and incorrectly ('three different call paths, which is what makes independent final validator real' \u2014 three callers of one pure function is zero independence). A's `verify_construct(record, prob.rules)` re-runs `rule.evaluate()`. C is best (I1\u2013I10 are hand-written and re-derive independently, and I9/I10 are genuinely new) but still leaves F1/F3 repeats, D1 beyond the frozen motif set, and the \u00a72.G size caps self-certified. critique Tier-7 Layer 1 requires 'an independent validator written against a different code path than the optimizer's scorer.'",
   "A ships a strand bug in the flagship use case. `ConstructRecord.packaged_strand` and `strand_view()` exist, but `EvalContext` has no strand field and `RuleSpec.strands` is a static tuple frozen in each rule file \u2014 A's own worked example ships `strands=(1,)`. For a reverse-oriented lentiviral cassette the packaged genome is the reverse complement (brief \u00a72.G, \u00a73.3), so the D4 `AATAAA`/`ATTAAA` hard-fail (8\u20139\u00d7 functional titer loss) and D3 donor scanning run on the wrong strand and return clean. Brief: 'Cassette orientation is a required input; without it the polyA/splice analysis is exactly backwards.'",
   "B cannot express a rule whose enforcement level depends on delivery context, because `RuleSpec.enforcement` is frozen once per rule id in a central catalogue. Brief \u00a72.D4 is 'H for lentiviral sense strand, S elsewhere'; \u00a73.2 makes cryptic splice donors HARD for lentiviral and genome-integrated but warn-only for plasmid; \u00a78.4 makes G4 hard for bacterial CDS and soft for mammalian mRNA. Under B, one of those two settings is wrong in every job, and `Finding` carries no role field to tell the user which context produced it \u2014 so the conflict panel that justifies the whole product is under-determined.",
   "C's `requires_construct: ClassVar[bool] = False` is a sanctioned opt-out from the product's central differentiator. Brief \u00a76.1: every competitor optimizes a free-floating linear CDS; BT5's claim is evaluation on the assembled circular product. C hands the rules lane a per-class switch that reverts any rule to bare-ORF evaluation for performance, losing junction-spanning and origin-spanning hits. Under eight parallel agents with a 10-second budget gate (G7) as the visible pressure, this switch WILL be flipped broadly, and nothing in CI detects it \u2014 G2 only exercises the three skeleton rules.",
   "B's `Recorded`-by-default plus `FakeFoldingBackend` (dg = \u22120.031 \u00d7 GC count) means every structure rule is authored and merged against a linear function of GC content, with `fixture-fidelity` explicitly nightly and non-blocking so a stale or wrong fixture never blocks a PR. B1's \u22124..+37 window, B3's Bo\u00ebl dual gate at \u221239 kcal/mol, and B11's \u221230/\u221250/\u221260 cap-proximal thresholds are all numbers whose meaning is defined by a real energy model. Thresholds tuned against the fake will be wrong in a way no PR-blocking check can see.",
   "B and C both formulate the objective as a monotone weighted sum over direction-corrected normalized values, with no defined handling for band objectives. `Direction.BAND` appears in B's enum and is never given a normalization; C's forced 'lower is always better' `raw_score` cannot represent one at all. CAI (C1), global GC (E2) and the F5 AT-window (45\u201360% GC per 100 nt, which brief \u00a72.F5 explicitly requires be resolved as a two-sided band with per-window binding-side display) are all bands. A monotone sum pushes each to an extreme \u2014 CAI toward 1.0, which brief \u00a71.1 shows collapses to one codon per amino acid and, per critique Tier-1 #4, produces perfect nucleotide repeats in exactly the antibody/scFv/linker/tag proteins people actually express.",
   "A and C both promise per-rule cost accounting ('enforcing D1 cost you 3 percentile points of 5\u2032 accessibility') and ranked relaxations with `predicted_cost` across every other objective. Both require N additional constrained solves. A budgets 0.5 s for 'report + assembly + vendor'; C budgets a 10 s end-to-end ceiling (G7) with no line item. Either the feature does not ship, or the numbers are estimated rather than measured and the report presents an estimate as a measurement \u2014 brief \u00a76.8 makes 'quantifies the cost of each constraint' a headline differentiator, so an unbudgeted approximation here is a dishonesty risk, not a scoping one.",
   "Only C treats the \u03bb-sweep gallery-collapse risk (its R4/G4), and it is a product-invalidating scientific risk that brief \u00a74 states outright: 'weighted-sum sweeps recover only supported (convex-hull) Pareto points.' With hard constraints doing most of the work, 24 \u03bb-points can collapse to a handful of near-identical sequences. A ships `lambda_sweep: int = 24` and `n_candidates \u2208 3..8`; B ships 'candidates: 3\u20138 genuinely different designs' as a docstring. Presenting five 98%-identical sequences as a trade-off panel \u2014 the honest answer to a field where optimizer choice is a coin flip (brief \u00a71.1, Ranaghan 2021) \u2014 is a dishonest UI built on an unmeasured assumption. Neither A nor B measures diversity or offers the \u03b5-constraint / COSMO vector-valued fallback brief \u00a74 names.",
   "A's per-segment `Segment.table_id` is biologically incoherent within a single reading frame and is a silent-wrongness generator. One ribosome translates one ORF under one genetic code; A states no invariant forbidding two DESIGNABLE_CDS segments in the same frame from carrying different `table_id` values, and because `verify_construct` re-derives translation per segment, a mis-set table produces the wrong protein and PASSES verification. This reintroduces critique Tier-1 #2 (the CTG=Ser class of bug) inside the very field added to prevent it."
  ]
 },
 {
  "scores": [
   {
    "proposal": "C-vertical-slice",
    "total": 30,
    "parallelBuildability": 7,
    "scientificCorrectness": 8,
    "riskManagement": 8,
    "concreteness": 7,
    "strengths": [
     "Only proposal that does the windowed-GC arithmetic instead of asserting a conclusion: 600 automaton states x 52 GC-count states = 31.2k states, ~92M relaxations, 0.5-1.5s vectorized \u2014 and it correctly labels block-wise GC as 'a strictly weaker surrogate for sliding-window' rather than pretending it satisfies brief 2.E3. Recommends Lagrangian steering + Tier-B repair + independent validator, and makes the owner RATIFY the weaker guarantee in week one instead of discovering it in week five.",
     "Names the risk nobody else named (R4/G4): a lambda-sweep over a weighted sum recovers only supported convex-hull Pareto points, so with hard constraints doing most of the work the 24-point sweep can collapse to 3 near-identical sequences. The brief says exactly this in section 4 and A and B both hard-code 'candidates: 3..8' into their type systems without ever asking whether the candidates will differ. G4 measures pairwise codon distance BEFORE the UI lane builds a gallery around the premise.",
     "Only proposal that budgets the null model. `Spec.requires_construct: ClassVar[bool]` lets the 200-500 variant null run cheap specs on the bare ORF and expensive specs only on the full plasmid, with a SQLite cache keyed on (protein sha256, host, table_id, spec-set version, N, seed) and G3 gating at <2s with an explicit fallback to report-time-only. Critique Tier-1 #3 is a latency bomb sitting outside brief section 4's 10s budget and C is the only one that defuses it.",
     "I9 ('every immutable interval is byte-identical to the input backbone') is the single best new invariant proposed in any of the three documents. It converts the worst possible bug in a vector-context tool \u2014 the optimizer silently edited the user's backbone \u2014 from a shipped plasmid into a raised exception. I10 (cassette frame across the assembled cassette, brief A4) is second best.",
     "Per-slot Spec instantiation: each Spec is instantiated once per gating ContextSlot, so a Breach always carries `slot_role` and two slots demanding opposite things produce two Breaches over the same Interval. Brief section 3.3's conflict surfacing becomes a consequence of the data model rather than a feature someone has to remember to build.",
     "The gates are numbers with named fallbacks, not assertions. G2 (real Addgene lenti GenBank round-trips byte-identical incl. origin-spanning join(); BsaI planted across the insert/backbone junction AND across the origin both caught; I9 catches a deliberate backbone edit) is the correct thing to be most scared of, and C says so.",
     "M6 pre-registration is the best anti-conflict device in any of the three: every dependency declared in pyproject at PR #0 (kills githubPlan 8.2's lockfile storm), every benchmark metric name pre-registered in tolerances.yaml with blocking:false (kills 8.6's baseline thrash and the label-gate serialization), full panel.json up front. A and B both leave these as incremental owner-gated edits that serialize every lane on the owner.",
     "In-lane `data/` with a mandatory `_provenance.json` sidecar for data that changes WHICH liabilities are flagged, root label-gated `data/` only for data that can silently change the PROTEIN. Smarter split than A's and B's monolithic label-gated data directory, and it matches the actual blast radius.",
     "`last_verified` as a required ClassVar rendering a stale-warning chip in the UI directly answers brief section 5's 'assume 12-month staleness \u2014 Twist changed homopolymer 14 to 30'. B has no date field at all.",
     "Pins a sign convention (`raw_score`: lower is always better). Neither A nor B does, and mixed-direction raw scores across ~70 rules written by 4+ agents is a guaranteed class of silent sign bugs."
    ],
    "weaknesses": [
     "'Never scan two strands \u2014 close the pattern set under revcomp and scan forward only' is stated as a blanket architectural rule and is wrong as one. It is correct for motif sets and false for every directional scored model in the brief: MaxEntScan donors/acceptors (2.D3), the Salis promoter calculator (2.D5), the TIR/RBS model (2.B6), and polyA hexamer-plus-downstream-element (2.D4) are not revcomp-symmetric. Encoded as a global invariant this becomes a correctness bug the moment lane 3 ships models/.",
     "Genetic code lives on `ContextSlot`, not per segment. An organelle-targeted fusion, or any construct mixing a nuclear ORF with a chloroplast-targeted one, is unrepresentable. A's per-`Segment.table_id` is strictly more correct and this is exactly the axis critique Tier-1 #2 says is a silent protein-changing bug.",
     "`Severity {HARD, SOFT, INFO}` cannot express two things the brief requires: 'pre-flight, not fixable by codon choice' (B10 uAUG in the user's UTR) and 'report-only descriptive statistic' (ENc, which brief 2.C6 says must NEVER be an objective). A's five-value Enforcement enum is the better contract and this one is frozen in PR #0.",
     "Wave 1 is 3 agents for two weeks against the user's explicit '8 parallel agents' premise. C argues the CI slot ceiling (githubPlan 8.1: ~5 concurrent sessions) makes the loss small, which is true, but 7 lanes genuinely idle for ~14 days is a real cost and C undersells it as purely a rebase-cost tradeoff.",
     "Everything hinges on the owner hand-writing a 4-6k-line working foundation and running seven gates in seven days. C is honest that this is the price, but there is no contingency: if week one becomes week three, seven agents wait. And the schedule contains no gate for the thing brief 7-tech-vi says to prove in week one \u2014 macOS hardened-runtime notarization of the PyInstaller sidecar.",
     "`ScoreCard`, `CassettePlan`, `Provenance`, `BiosecurityReport` and `HostDescriptor` are named in signatures and never specified, and there is no worked rule file showing the pattern a rule agent copies. A is materially more concrete at the code level."
    ]
   },
   {
    "proposal": "A-domain-layered",
    "total": 25,
    "parallelBuildability": 7,
    "scientificCorrectness": 6,
    "riskManagement": 4,
    "concreteness": 8,
    "strengths": [
     "Best concreteness of the three by a clear margin. Real code, real signatures, a fully worked rule file with a consequence table showing what nine downstream consumers gain for free, a real host TOML, explicit path_guard.RULES amendments, explicit new CI job names. This is the one you could start typing tomorrow.",
     "Rule filenames map 1:1 onto brief section 2 IDs (`d8b_cpg_zap_window.py`, `f5_at_window_band.py`). 'Did we implement D5(c)?' is answered by `ls`. Trivial-sounding and genuinely the difference between a reviewable and an unreviewable ~45-file catalog.",
     "`Enforcement {HARD_LATTICE, HARD_REPAIR, HARD_CHECK, SOFT, REPORT_ONLY}` is the best single type in any of the three documents: it is the machine-readable form of the brief's 'hard constraints are guaranteed by construction or by reject-and-repair plus an independent validator, never by penalty weights', and pipeline.py asserts a HARD rule can never be routed to the weighted sum. HARD_CHECK also gives B10 (uAUG, unfixable by codon choice) a home, which C's three-value Severity does not have.",
     "`RuleSpec.engine_calibration` refuses a ViennaRNA-calibrated threshold at load time if the active FoldEngine reports different (name, params), degrading to reporting the metric WITHOUT the threshold. This is the sharpest available answer to brief 7-tech-iii, which names silently applying a ViennaRNA threshold to seqfold output as the most likely correctness bug in the whole feature \u2014 and it is enforced in core/ so no rule author can forget.",
     "`LocalizationPolicy` as data (motif_len-1 / window-1 / whole_scope / paired_segments) turns DnaChisel's imperative localization heuristics into a declaration Tier B consumes generically. The solver never learns biology; the rule never learns the solver.",
     "`Segment.table_id` per segment is the most correct genetic-code modeling of the three \u2014 it is the only one that can represent an organelle-targeted fusion.",
     "`NullScreen` returns status='not_run' and NEVER 'clear', and the ScreenResult is embedded in DesignProvenance, printed on the QC report, and written to the output GenBank COMMENT. A user who ships an unscreened design carries a permanent record saying so. That is a better honesty design than B's HMAC token, which pretends to be a security control in a locally-installed app the user can edit.",
     "`Violation.fixable_by_codon_choice: bool` routing unfixable findings to the protocol/relaxation advisor is a small field that correctly separates 'the optimizer failed' from 'no codon choice can fix your UTR'.",
     "The rule contract test (evaluate() pure, every Violation.interval inside the construct, HARD_LATTICE implies non-empty lattice_motifs, SOFT implies non-empty weight_provenance, >=1 Citation, Evidence.C implies default_enabled False) means a rule agent gets its entire test suite for free and structurally cannot ship a rule missing an evidence badge."
    ],
    "weaknesses": [
     "Self-contradictory on the single hardest algorithmic question. Section 3.4 says the two rules needing a new DP state dimension are 'windowed GC history ~17 codons' and 'codon-pair state' and that 'both ship in PR #0'; section 14.3 then admits windowed GC has no literature precedent, is declared HARD_REPAIR, and is handled in Tier B. brief 7-tech-i says naive state augmentation is intractable. It cannot be both, and A never produces a number either way.",
     "The reference implementation every rule agent will copy contains the exact bug the brief warns about. `d8b_cpg_zap_window` declares `strands=(1,)` on a rule gated to lentiviral/AAV/IVT while `ConstructRecord` carries `packaged_strand`. For a reverse-oriented cassette that scans the wrong strand \u2014 brief section 3.3: 'without cassette orientation the polyA/splice analysis is exactly backwards.' Copy that pattern 40 times and orientation handling is wrong across the catalog.",
     "The same showcase rule's spacing gate is near-vacuous at its own defaults: 14 CpGs inside a 200-nt window force mean spacing <= 199/13 = 15.3 nt, so the `max_mean_spacing_nt: 14.0` filter almost never fires independently of the count filter. The rule the brief calls out as publishable-quality (ZAP density AND spacing) collapses to density.",
     "Worst risk management of the three. There are no gates, no measurements, and no spike. Section 14 lists four weaknesses and answers each with an assertion. The performance table in section 12 is a budget, not a result.",
     "The walking skeleton deliberately misses the highest-blast-radius axis: the foundation's `construct` stub is 'linear concatenation, correct frame math'. So optimize-in-the-context-of-the-assembled-circular-plasmid \u2014 the competitive differentiator and the thing every rule signature depends on \u2014 is never exercised before eight agents write rules against `ConstructRecord`. A claims the type prevents the free-floating-CDS bug; it prevents the API shape, not the coordinate bugs.",
     "Per-rule cost accounting ('enforcing D1 cost you 3 percentile points and changed 11 codons') requires one re-solve per hard rule. A budgets the entire report+assembly+vendor stage at 0.5s while a Tier-B repair is budgeted at 4s. With ~40 hard rules this is minutes, not half a second.",
     "The null-model line 'ceq 1s first run' for 400 variants is asserted with no arithmetic and no cost-control mechanism. At RNAfold ~0.24s/kb a single full fold per variant is ~96s, and A has no equivalent of C's `requires_construct` flag to keep expensive specs off the null.",
     "Ignores githubPlan 8.1 entirely and proposes 8 lanes scaling to 10 against a 20-slot cap where one Python PR costs ~12 jobs. The parallelism argument is priced at zero; the plan's own analysis says cap at 4-5 sessions.",
     "Does not name the gallery-degeneracy risk, despite building `n_candidates: 3..8` into DesignProblem and asserting 'the API has no single-design endpoint.'"
    ]
   },
   {
    "proposal": "B-contract-first",
    "total": 23,
    "parallelBuildability": 8,
    "scientificCorrectness": 5,
    "riskManagement": 4,
    "concreteness": 6,
    "strengths": [
     "The clearest statement of the actual goal \u2014 'zero import-time edges between agent-owned packages, and a running typed non-trivial input at minute zero' \u2014 and the mechanisms genuinely serve it. `mypy --strict` per package with no edges between M1..M7 means no lane's refactor can turn another lane's CI red. That property is real and neither A nor C states it this precisely.",
     "Per-lane golden directories (`tests/goldens/<lane>/__snapshots__/`) and per-consumer fixture namespacing (`fixtures/ports/<port>/<consumer_lane>/<call_hash>.json`) are the sharpest conflict analysis in any of the three. githubPlan's `goldens-not-hand-edited` deletes and regenerates all snapshots, so one shared `__snapshots__` dir means every PR from every lane touches it \u2014 a guaranteed conflict storm at 6+ concurrent PRs. A and C both leave this unaddressed.",
     "Consumer-driven contract testing across the M8/M9 seam: the SAME recorded HTTP transcripts are served by tools/mockserver for the web lane and replayed against the real ASGI app via httpx.ASGITransport for the server lane, both validated against the frozen OpenAPI schema. Neither lane can drift without one going red, and neither waits for the other to exist.",
     "`FoldEnergy` carrying (engine, engine_version, param_set, temperature_c, dangles) in the VALUE type \u2014 not in a side-channel provenance record \u2014 is the strongest structural answer to brief 7-tech-iii. A fake or seqfold number is un-mistakable at every downstream call site, including inside a golden. Better than A's load-time gate, and they compose.",
     "`ProteinInput.translation_table_id` with no default: Pydantic refuses to construct the model. Critique Tier-1 #2 becomes structurally impossible rather than a convention someone remembers. Same device on `DesignRequest.screen_token` and `DesignRequest.seed`.",
     "Pre-declaring the full ~70-entry RuleSpec catalogue at PR #0 separates the rule NAMESPACE (frozen, owner-owned, never a merge conflict) from the rule IMPLEMENTATION (one self-registering file). A spec with no implementation is a legal reported TODO, so the UI renders the complete brief-section-2 rule list on day one and the web lane is never behind the rules lanes.",
     "The CI-enforced ban on reading `ObjectiveValue.raw` outside metrics/ and output/ is the right teeth for critique Tier-1 #3. Sliders and solver act on `normalized` only, so linearity is a checked property rather than a hope.",
     "Honest, correct read of githubPlan 8.1: nine lanes are nine ownership boundaries, concurrency is throttled by CI slots to ~4-5, and because there are no dependency edges the running subset is arbitrary and rotatable at zero cost. A ignores this entirely.",
     "Section 7.5 is the most intellectually honest passage in any of the three: the real defense against churn is spending PR #0 on the SHAPES of the four expensive conventions (coordinates, units, error taxonomy, RuleKind), not the count of fields \u2014 and `Feature.parts: tuple[Interval, ...]` chosen anticipatorily converts a MAJOR amendment into no amendment at all."
    ],
    "weaknesses": [
     "`RuleSpec` has no parameters, no units, no band, no thresholds, and no last-verified date. Rule thresholds are the literal subject matter of every slider ('user-controlled trade-off weighting' is the product) and the thing vendor staleness invalidates \u2014 brief section 5, Twist homopolymer 14 to 30. Frozen across ~70 specs at PR #0, adding them is a MAJOR amendment; until then window_nt, min_cpg and every threshold live in `ext`, i.e. the shadow API. The UI also cannot render a parameter control generically, which A and C both solve (A via `parameters` + LocalizationPolicy, C via `param_schema: JSONSchema7`).",
     "Freezes `RuleKind` \u2014 explicitly named the solver/rules dispatch boundary and placed in the never-amend list \u2014 at PR #0, then schedules the 48-hour windowed-GC spike that determines whether RuleKind is right for AFTER nine lanes have started. B's own section 10 concedes a fifth kind is a MAJOR amendment touching M1, M2 and M3 simultaneously. The document's central bet contradicts its own top risk.",
     "The day-zero fixture corpus has no producer. B ships 'every signature and zero bodies', yet PR #0 promises ~20 recorded DesignCandidates, parsed ConstructRecords for 8 real Addgene backbones, and full HTTP transcripts. Recording requires an implementation. So those payloads are hand-fabricated guesses, nine lanes develop against them for weeks, and they are wrong the first time a real GenBank parser runs. `fixture-fidelity` is nightly and deliberately NON-BLOCKING, so the divergence surfaces as an issue, not a stop.",
     "No performance analysis anywhere in the document, for a product whose defining constraint is brief section 4's 10-second interactive budget. Tier A/B/C get one table cell each. `/design/preview` asserts 500ms and `/rescore` asserts 20ms with no supporting arithmetic, and the 200-500 variant `Normalizer.build_null` \u2014 which `ObjectiveValue.percentile` makes REQUIRED on every objective \u2014 is entirely unbudgeted.",
     "`extra='forbid'` plus a universal `ext: dict[str, JsonValue]` bag on every cross-lane model is `extra='allow'` with extra steps. B admits in section 10 risk 2 that a determined lane could live in `ext` forever and the 14-day check is 'a report, not a gate'. Under nine lanes and schedule pressure, `ext` is where the contract actually lives.",
     "The contract is presented as complete and is not. `DesignContext`, `HostProfile`, `WeightVector`, `ApplicationPreset`, `CassettePlan`, `Recommendation` and `RelaxationOption` are referenced repeatedly and never defined; the ~70-entry catalogue is asserted, not shown. For a proposal whose entire thesis is 'the contract IS the architecture, frozen at PR #0', the unshown half is the half containing the science.",
     "PR #0 is by far the largest of the three \u2014 a full Pydantic contracts package, 70 hand-transcribed RuleSpecs, the kernel, the protein/biosecurity/code-table layer, a working ReferenceSolver, every FastAPI route, the corpus, the mockserver, and generated contracts-ts \u2014 and B never estimates it. C at least prices its foundation at 4-6k lines and owns the cost.",
     "The HMAC `screen_token` is framed as a security control. In a fully offline locally-installed app the signing key ships with the binary; this is a forcing function against accidental omission (valuable) dressed as a gate against misuse (it is not). A's NullScreen-that-never-returns-clear plus a permanent record in provenance and the GenBank COMMENT is the more honest version of the same idea.",
     "No differential/metamorphic story beyond 'M1 vs ReferenceSolver'. Critique Tier-7 Layer 2 names constraint-order-independence and redundant-constraint-invariance as a ready-made bug class (DnaChisel fails it); B assigns tests/differential/ to M1 and never specifies the relations."
    ]
   }
  ],
  "winner": "C-vertical-slice",
  "mustGraft": [
   "A's Enforcement enum {HARD_LATTICE, HARD_REPAIR, HARD_CHECK, SOFT, REPORT_ONLY} replacing C's Severity {HARD, SOFT, INFO}, plus the pipeline.py assertion that a HARD rule can never be routed to the weighted sum. C's three-value enum cannot express B10 (uAUG pre-flight, unfixable by codon choice) or C6 (ENc, which brief 2.C6 says must never be an objective), and both are in v1 scope.",
   "A's per-Segment.table_id replacing C's per-ContextSlot table_id, combined with B's no-default enforcement (a model that cannot be constructed without an explicit translation table). Per-slot alone cannot represent an organelle-targeted fusion; a defaulted field is exactly the silent protein-changing bug of critique Tier-1 #2.",
   "B's FoldEnergy value type carrying (engine, engine_version, param_set, temperature_c, dangles) AND A's engine_calibration load-time refusal (degrade to reporting the metric without the threshold when the active engine's calibration key does not match), on top of C's G6 measurement of whether the seqfold/ViennaRNA offset is constant. All three together, because brief 7-tech-iii names this the most likely correctness bug in the whole feature. C's bare `FoldEngine.params` ClassVar is not enough \u2014 the dG must carry its engine everywhere it travels, including into goldens.",
   "B's pre-declared rule-ID namespace as an owner-owned manifest at PR #0 (id, title, brief_ref, evidence, default_enabled only), with the full spec living in the rule's own file. This gets B's day-one complete rule list in the UI with 'not yet implemented' states and guaranteed ID uniqueness, without B's mistake of freezing 70 rules' worth of solver dispatch and thresholds before anyone has implemented one.",
   "A's 1:1 filename mapping onto brief section 2 IDs (d8b_cpg_zap_window.py, f5_at_window_band.py) plus 2-3 hand-written reference rule files in PR #0 that every rule agent copies. C has registry autodiscovery but no naming convention and no worked example \u2014 with ~40 rules across two lanes that is the difference between a reviewable and an unreviewable catalog. Fix A's strand bug before making it the template: the reference rule must read packaged_strand from the record, never hard-code strands=(1,).",
   "A's LocalizationPolicy as declared data (MOTIF_LEN_MINUS_1 / WINDOW_MINUS_1 / WHOLE_SCOPE / PAIRED_SEGMENTS) consumed generically by Tier B. C's `Spec.localize(iv)` is a per-rule callable, which means 40 agents each hand-roll DnaChisel's localization heuristics and get the two-overlapping-breaches suppression rule wrong independently.",
   "A's Violation.fixable_by_codon_choice flag on C's Breach, routing unfixable findings (uAUG in the user's UTR, protein-level toxicity, AAV size overflow) to the protocol/relaxation advisor rather than to the solver.",
   "A's NullScreen contract: status='not_run', never 'clear', with the ScreenResult embedded in Provenance, printed on the QC report, and written into the output GenBank COMMENT. Plus B's device of making the screen result a REQUIRED non-defaulted field on the design request so it cannot be forgotten \u2014 but drop B's HMAC framing, which is not a security control in a locally-installed app.",
   "B's per-lane golden/snapshot directories and per-consumer fixture namespacing (tests/goldens/<lane>/__snapshots__/, tests/fixtures/ports/<port>/<consumer_lane>/). githubPlan's goldens-not-hand-edited regenerates every snapshot, so a shared directory is a guaranteed conflict on every PR from every lane. C's plan leaves this shared and its M6 pre-registration section does not cover it.",
   "B's contract-freeze and openapi-drift CI jobs (committed schema JSON + committed generated TS, regenerate-and-diff, failing without an approved:contract-* label) and B's consumer-driven HTTP transcripts replayed from BOTH sides \u2014 mockserver for the web lane, httpx.ASGITransport for the server lane. C's 'generated TS with a staleness job' is the same idea at half the strength.",
   "B's ReferenceSolver as a permanent differential oracle \u2014 deliberately dumb, exhaustive over short windows, slow, never wrong \u2014 kept forever rather than thrown away. C has no differential oracle of its own and githubPlan's nightly DnaChisel differential is a weaker substitute (DnaChisel itself fails constraint-order-independence, so disagreements are ambiguous).",
   "A's null-model cache key (protein_hash, host, ruleset_hash) and the observation that the same 200-500 variants double as critique Tier-7 Layer 4's random-synonymous-shuffle negative control at zero extra cost. C caches on a similar key but never connects it to the negative control, which is a required validation layer.",
   "A's banned-schema-field grep run against the GENERATED OpenAPI (predicted_expression, expression_score, titer_prediction, fold_improvement) plus B's contract test asserting no field name matches /predict|titer|yield|expression_level/. Brief section 7-i and critique Tier-5 #10 make this the honesty guarantee; grepping the generated schema catches what grepping source misses.",
   "A's explicit scale-ceiling statement with FVIII (2332 aa) and dystrophin (3685 aa) as corpus members so the graceful-degradation path (windowed-only structure above N, degradation recorded in Provenance) is exercised in CI rather than discovered by a user. C's panel.json includes FVIII but C never states a ceiling or a degradation contract.",
   "B's ext-bag escape hatch, narrowed: permitted only on report/detail types, banned on anything the solver or the sliders read, with the 14-day debt list gating something real (e.g. promotion of claude-review-gate to required). C's provisional-freeze wave handles interface churn for 3 lanes for 2 weeks but has no mechanism at all for wave 2, where 7 lanes hit a hard-frozen core."
  ],
  "fatalFlaws": [
   "ALL THREE: there is no packaging lane, no packaging gate, and no install story. Brief section 4 pins the shipped artifact as Tauri v2 + React + a PyInstaller --onedir CPython sidecar on 127.0.0.1, and brief 7-tech-vi says to prove macOS hardened-runtime notarization (com.apple.security.cs.allow-unsigned-executable-memory, disable-library-validation) with a hello-world sidecar IN WEEK ONE. All three stop at 'FastAPI on 127.0.0.1', which is the dev shape. Nobody owns notarization, code signing, the ~100 MB no-delta-update payload (brief 7-tech-vii), Windows/Linux equivalents, the optional ViennaRNA install, or the commec biosecurity database download \u2014 which A calls 'an optional one-time DB download' and C calls 'an optional extra' without either sizing it (it is BLAST-nr-derived and multi-GB). A scientist cannot install any of these three plans. This must be lane zero with a week-one gate, and it is missing from all three.",
   "PROPOSAL A is self-contradictory on the hardest algorithmic question in the project. Section 3.4 asserts the two DP state dimensions \u2014 'windowed GC history ~17 codons' and codon-pair state \u2014 'both ship in PR #0'; section 14.3 then says windowed GC has no literature precedent, is declared HARD_REPAIR, and is handled in Tier B. brief 7-tech-i says naive state augmentation is intractable. A produces no number either way. Shipping the foundation on the first reading and the rules on the second is how you get 40 rule files written against a solver contract that changes in week four.",
   "PROPOSAL A's showcase rule \u2014 the file every rule agent copies \u2014 encodes the exact orientation bug the brief warns about. `d8b_cpg_zap_window` declares strands=(1,) and gates on lentiviral/AAV/IVT while ConstructRecord carries `packaged_strand`. Brief section 3.3: 'the lentiviral genome and the target-cell mRNA are the same strand in a forward-oriented cassette but opposite strands in a reverse-oriented one... without it the polyA/splice analysis is exactly backwards.' Replicated across ~45 rule files by two agents, this is a systematic scientific error, and A's own contract test suite does not check it.",
   "PROPOSAL B freezes RuleKind \u2014 declared the solver/rules dispatch boundary and placed in the four-item never-amend list \u2014 at PR #0, then schedules the spike that determines whether RuleKind is correct for the first 48 hours AFTER all nine lanes have started. B's own section 10 concedes a fifth kind is a MAJOR amendment hitting M1, M2 and M3 at once. Under 8-9 agents this is the single highest-blast-radius decision in the document and B sequences it backwards.",
   "PROPOSAL B's frozen RuleSpec has no parameters, no units, no band, no thresholds and no last-verified date. The entire product premise is user-controlled per-rule trade-off weighting, and brief section 5 says vendor thresholds go stale within 12 months (Twist homopolymer 14 to 30). Every threshold in ~70 rules would have to live in `ext` \u2014 the shadow API B's own section 10 admits it cannot police \u2014 until a MAJOR amendment across all 70 specs. The UI also cannot render a parameter control generically, so the web lane blocks on rules after all.",
   "PROPOSAL B's day-zero corpus has no producer. B ships 'every signature and zero bodies' yet PR #0 promises ~20 recorded DesignCandidates, parsed ConstructRecords for 8 real Addgene backbones (lenti forward and reverse, AAV with ITRs, an origin-spanning feature) and full HTTP transcripts. Recording requires an implementation; these are hand-fabricated guesses. Nine lanes then build for weeks against a guess at the interface payload, and `fixture-fidelity` \u2014 the job that would catch the divergence \u2014 is nightly and DELIBERATELY non-blocking. This is B's walking skeleton and it does not walk.",
   "CUT FROM V1 \u2014 the vendor-oracle CI job (A's nightly `vendor-oracle`, B's M7 tests/vendor_oracle/). Submitting designs to the Twist/IDT/GenScript order-entry complexity checkers means automating authenticated commercial web order forms: it needs accounts, it is scraping against ToS, it breaks silently on any form change, and it cannot be a dependable gate. Critique Tier-7 Layer 6 is right that it is the best available external label \u2014 as a manual, periodic calibration exercise the owner runs, not as CI infrastructure two lanes depend on. C correctly omits it.",
   "CUT FROM V1 \u2014 the assembly/order-file lane's back half. Vendor complexity PRE-CHECKS (brief 2.E1-E12) are just rules and stay. But Golden Gate overhang design with tatapov, Type IIS domestication planning, Gibson/NEBuilder/In-Fusion arm design, primer design, fragment splitting, and vendor order-file emission (A's M6, B's M7, C's L6) is a second product in DnaCauldron/GoldenHinges territory, orthogonal to the actual differentiator (constraint evaluation on the assembled circular construct), and it consumes a full lane in all three plans. V1 ships an annotated GenBank plus a checksummed ordering FASTA. Keep only Golden Gate destination AUTO-DETECTION (brief 6.2) because it is an input-parsing feature the vector lane needs anyway.",
   "CUT FROM THE INTERACTIVE PATH \u2014 per-rule cost accounting (brief 6.8: 'enforcing D1 cost you 3 percentile points and changed 11 codons'). A implements it as one re-solve per hard rule at report time while budgeting the entire report+assembly+vendor stage at 0.5s against a 4s Tier-B repair; with ~40 hard rules that is minutes. C's `Relaxation.predicted_cost` (delta percentile on EVERY other objective) has the same unbudgeted cost. This is a background job the user opts into for one rule at a time, or it is cut.",
   "OBJECTIVE NORMALIZATION IS UNBUDGETED IN A AND B. Critique Tier-1 #3 makes 200-500 synonymous-variant percentile normalization mandatory, and it sits entirely outside brief section 4's 10-second budget. A asserts '<=1s first run, ~0 after' for 400 variants with no arithmetic \u2014 at RNAfold ~0.24s/kb one full fold per variant is ~96s, and the 5' dG window term (the highest-weight objective in the whole model) has to be in the null or the sliders are dead for the objective that matters most. B makes `percentile` a REQUIRED field on every ObjectiveValue and never mentions cost. Only C engineers it (requires_construct, bare-ORF evaluation for cheap specs, SQLite cache, G3 gate with a report-time-only fallback).",
   "PROPOSAL A ignores githubPlan section 8.1 completely and proposes 8 lanes scaling to 10 against a free-account cap of 20 concurrent jobs where one Python PR consumes ~12. That is 4-5x oversubscribed; the plan's own conclusion is 'cap agent parallelism at 4-5 sessions'. A's parallelism argument is priced at zero and its 10-lane scale-out is fiction under the stated CI budget. B and C both engage with this honestly.",
   "PROPOSAL C's 'never scan two strands \u2014 close the pattern set under revcomp and scan forward only' is correct for motif sets and wrong as the blanket architectural invariant C states it as. MaxEntScan donors/acceptors (2.D3), the Salis promoter calculator (2.D5), the TIR/RBS model (2.B6) and polyA hexamer-plus-downstream-element (2.D4) are directional scored models whose scores are not revcomp-symmetric. Encoded globally in core/ this becomes a silent correctness bug the moment lane 3 ships models/. The rule must be scoped to LatticeTerms.forbidden, with scored models declaring their strand explicitly from packaged_strand."
  ]
 }
]